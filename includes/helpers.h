#ifndef HELPERS_H
# define HELPERS_H

# include <stdbool.h>
# include <stddef.h>
# include <stdint.h>

size_t 	strlen(const char* str);
int	strncmp(const char *s1, const char *s2, size_t n);

#endif

